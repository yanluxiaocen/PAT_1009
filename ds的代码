#include <stdio.h>

int main() {
    char a[80][80];  // 最多 80 个单词，每个最长 80
    int k = 0;

    while (scanf("%s", a[k]) != EOF) {
        k++;
    }

    for (int i = k - 1; i >= 0; i--) {
        printf("%s", a[i]);
        if (i > 0) printf(" ");
    }
    printf("\n");

    return 0;
}
